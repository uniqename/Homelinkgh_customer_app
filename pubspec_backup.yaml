name: homelink_ghana
description: HomeLinkGH - Ghana's unified platform for customers, providers, and admins. Complete home services ecosystem.
version: 3.1.0+11
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # Minimal dependencies for TestFlight
  http: ^1.2.2
  shared_preferences: ^2.3.3
  
  # Navigation & Routing
  go_router: ^14.6.1
  
  # International & Localization
  intl: ^0.20.2
  
  # Utilities
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true